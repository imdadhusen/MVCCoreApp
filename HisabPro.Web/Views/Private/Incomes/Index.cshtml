@model HisabPro.Web.ViewModel.GridViewModel<object>
@{
    ViewData["Title"] = "Incomes";
    Layout = "~/Views/Shared/_PrivateLayout.cshtml";
}

@* <div class="d-flex justify-content-between align-items-center mb-3">
    <div>
        <h1 class="h4 text-primary">Manage Incomes</h1>
        <p class="text-muted mb-0">
            <i class="bi bi-exclamation-circle-fill text-warning me-2"></i>
            By default, the most recent incomes are displayed first.
        </p>
    </div>
    <a asp-action="Save" class="btn btn-success">
        <i class="bi bi-plus-lg me-1"></i> Create New
    </a>
</div> *@

<div id="tblIncomes" style="position:relative">
    @await Html.PartialAsync("~/Views/Shared/_GridView.cshtml", Model)
</div>

@section Scripts {
    <script>
        $('#tblIncomes').gridview({
            controllerName: 'Incomes',
            titleDelete: 'Delete Income'
        });
    </script>
}