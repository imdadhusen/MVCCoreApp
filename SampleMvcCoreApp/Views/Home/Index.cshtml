@{
    ViewData["Title"] = "Home Page";
}
@model List<SampleMvcCoreApp.DTO.EmployeeDTO>
@* @model List<SampleMvcCoreApp.Entities.Employee> *@

<div class="text-center">
    <h1 class="display-4">Welcome</h1>
    <p>Learn about <a href="https://learn.microsoft.com/aspnet/core">building Web apps with ASP.NET Core</a>.</p>
    <ul>
        @foreach (var m in Model)
        {
            <li>
                @m.Id | @m.Name | @m.Skill | <a href="#">@m.Email</a> | @m.Department.Name | @m.IsActive | @m.IsDeleted
                <ul>
                    @foreach (var a in m.Address)
                    {
                        <li>@a.AddressInfo</li>
                    }
                </ul>
            </li>
        }
    </ul>
    @* <ul>
    @foreach (var m in Model)
    {
    <li>
    @m.Id | @m.EmployeeName | @m.Skill | <a href="#">@m.Email</a> | @m.Department.DepartmentName | @m.IsActive | @m.IsDeleted
    <ul>
    @foreach (var a in m.EmployeeAddress)
    {
    <li>@a.AddressDetail</li>
    }
    </ul>
    </li>
    }
    </ul> *@

</div>
